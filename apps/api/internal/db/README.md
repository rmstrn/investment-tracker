# internal/db

Database layer — will contain:

- `migrations/` — goose-managed SQL migrations (TASK_03)
- `sqlc/` — sqlc-generated Go code from queries (TASK_04)
- `pool.go` — pgx connection pool setup (TASK_04)

Empty at scaffold stage.
