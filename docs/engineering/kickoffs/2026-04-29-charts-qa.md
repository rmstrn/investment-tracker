# Kickoff — Charts QA v1 (Vitest + visual regression + Lane-A runtime guardrails)

**Date:** 2026-04-29
**Author:** tech-lead (Right-Hand) — QA kickoff completed by Right-Hand after tech-lead's session timed out
**Owner:** qa-engineer
**Status:** APPROVED BY PO — ready to dispatch
**Slice ID:** SLICE-CHARTS-QA-V1
**Branch:** `feat/charts-qa-v1`
**Base SHA:** verify on dispatch — current `main` tip after architect ADR + finance audit + FE + backend slices land. Check `git log --oneline -5 origin/main` first; do NOT start until at least the architect ADR, finance audit, and `feat/charts-backend-v1` are on `main`. FE merge can run in parallel with QA test authoring (you're testing the contract, not the rendering).
**Worktree:** main repo working tree (no separate worktree needed; sister kickoffs run on independent branches)
**Scope sizing:** medium surface — single PR with three layers (parser tests, visual regression baselines, runtime guardrail tests). Quality gate: 11 chart × 2 theme = 22 baseline screenshots locked; finance audit's «implementation guardrails» translated into runtime checks; CI green; flaky-test rate at zero before merge.
**PR target:** `main`; single PR

---

> **Cross-team coordination**
>
> **Architect ADR:** `docs/reviews/2026-04-29-architect-chart-data-shape-adr.md` — read first. Defines `ChartEnvelope` shape, `parseChartEnvelope` boundary, Zod schema location. Your parser-tier tests target this contract.
>
> **Finance audit:** `docs/reviews/2026-04-29-finance-charts-lane-a-audit.md` — read second. Section §4 «Implementation guardrails (cross-cutting)» enumerates the runtime invariants you must encode as block-CI tests. Section §3 «Detailed findings» has per-chart `[SEVERITY]` items — every CRITICAL and HIGH translates to a test case.
>
> **Theme decision:** `data-theme="light"` / `data-theme="dark"` on `<html>` per `docs/DECISIONS.md` 2026-04-29 entry. Visual regression baselines are captured in BOTH themes (22 baselines: 11 charts × 2 themes); theme is set via DOM mutation in test setup (`document.documentElement.setAttribute('data-theme', 'dark')`), not class toggle.
>
> **FE kickoff:** `docs/engineering/kickoffs/2026-04-29-charts-fe.md` — read third. Defines which 11 components exist, their export paths, T1/T2/T3 lazy boundaries. Your visual baselines mirror FE's component inventory exactly.
>
> **Backend kickoff:** `docs/engineering/kickoffs/2026-04-29-charts-backend.md` — read fourth. Defines schema package location and `parseChartEnvelope` API surface. Your parser tests live alongside backend's schemas (preferred) or in a sibling `__tests__/` directory.

---

## 1. Why this slice

A chart subsystem fails silently in three ways: (a) the parser accepts malformed payloads and the renderer crashes downstream, (b) a theme regression turns a forest-jade gain into a bronze loss without anyone noticing, (c) a Lane-A guardrail (info-only chip, projection caption, staleness banner) gets removed during refactor and the product silently starts looking like advice. Unit tests catch (a). Visual regression catches (b). Runtime guardrail tests catch (c).

This slice ships all three layers in one coordinated test suite that pins chart correctness for the indefinite future. It is the safety net that lets us merge subsequent chart changes (new variant additions, theme tweaks, Recharts upgrades) without burning a multi-agent review every time.

---

## 2. Hard rules (apply throughout)

- **R1 — No spend.** Visual regression strategy is **local Playwright screenshot diff** (Playwright is already installed; the diff comparator is a small first-party utility). Do NOT subscribe to Chromatic, Percy, BackstopJS-cloud, Argos-CI, or any visual-regression SaaS. Do NOT add a paid CI tier. Vitest is already installed; add no new test runners. **Any test infrastructure that costs money requires explicit per-transaction PO approval.**
- **R2 — No external comms.** No tweets, blog posts, vendor outreach, framework-maintainer DMs authored on PO's behalf during this slice. Drafts for PO review are fine; sending as PO is not.
- **R4 — No predecessor references.** Do not surface the rejected naming predecessor in commit messages, test names, fixture file names, or PR description.

These rules are cited verbatim in every kickoff in this trio.

---

## 3. Theme mechanism (decided 2026-04-29)

`data-theme="light"` / `data-theme="dark"` on `<html>`. See `docs/DECISIONS.md` 2026-04-29 entry.

Test setup pattern:

```ts
// In Playwright fixture or Vitest setup
beforeEach(({ theme }: { theme: 'light' | 'dark' }) => {
  document.documentElement.setAttribute('data-theme', theme);
});
```

Visual baselines must be captured under BOTH themes. A regression in either is a block-CI failure.

---

## 4. Source-of-truth inputs (read in this order — do NOT skip)

1. **`docs/reviews/2026-04-29-architect-chart-data-shape-adr.md`** — `ChartEnvelope` shape + `parseChartEnvelope` API. Your parser tests assert on this exact contract.
2. **`docs/reviews/2026-04-29-finance-charts-lane-a-audit.md`** — Lane-A guardrails to encode as runtime tests. §4 «Implementation guardrails» is your test-case specification.
3. **`docs/reviews/2026-04-27-chart-implementation-blueprint.md`** — code-architect's blueprint, especially §«AI-agent integration boundary» (parser tier) and §«Bundle size» (lazy-load tests).
4. **`docs/design/CHARTS_SPEC.md`** v1.1 — §5.2 (Zod schema) + §5.3 (example payloads — use as fixture seed) + §3 (universal anatomy — empty / loading / error states).
5. **`docs/product/chart-component-catalog.md`** v1.1 — surface-mapping cross-check; for each chart, the catalog says where in the product the chart appears; your visual baselines should reflect at least one realistic data fixture per surface.
6. **`docs/engineering/kickoffs/2026-04-29-charts-fe.md`** — FE's component inventory + export paths.
7. **`docs/engineering/kickoffs/2026-04-29-charts-backend.md`** — backend's schema package location.
8. **`packages/ui/src/charts/`** — FE's implementations (after FE merges).
9. **`packages/shared-types/`** (or wherever Zod schemas land per architect ADR) — backend's parser.

If any of items 1 / 2 / 6 / 7 are missing when you pick this up — STOP and surface to Right-Hand. They are running in parallel and MUST be on `main` before you begin.

---

## 5. Scope

### IN scope — three test layers

**Layer A — Parser tier (Vitest, fast, no DOM):**

- For each of 11 chart kinds: a positive test (valid payload parses successfully and produces the expected discriminated-union variant).
- For each of 3 Risk Flags from CHARTS_SPEC §5.2: a negative test (payload violating that flag is rejected by the parser; assert on the Zod issue path so future schema refactors don't accidentally weaken the check).
- For each CRITICAL or HIGH finding from finance audit §3: a negative test that the parser rejects the violating payload (e.g., waterfall conservation invariant, OHLC ordering, sum-to-total invariants for Donut / Treemap / StackedBar, calendar `Currency` required-not-defaulted).
- Snapshot test of the discriminated-union TypeScript output for documentation stability.

**Layer B — Visual regression (Playwright, screenshot diff):**

- 11 charts × 2 themes = 22 baseline screenshots, captured at a single fixed viewport (1280×800).
- Each baseline uses a realistic fixture from CHARTS_SPEC §5.3 example payloads (no synthetic «hello world» fixtures — the realism reduces false-positive surprise on real-data renders).
- Diff threshold: 0.1% pixel difference (anti-aliasing tolerance). Any regression above threshold is a block-CI failure.
- Baselines stored in `tests/visual/baselines/<chart-kind>__<theme>.png`. Diff artefacts (when failures occur) saved to `tests/visual/__diffs__/` and uploaded as CI artefacts for review.
- A CLI `pnpm test:visual:update` regenerates all baselines (used after intentional visual changes). The regeneration step requires explicit local opt-in — never run automatically in CI.

**Layer C — Runtime guardrail tests (Vitest + Testing Library, DOM-aware):**

For each cross-cutting guardrail in finance audit §4, encode a runtime test:

1. **Info-only chip** — render a chart with `chip: 'info-only'` (or whatever the guardrail field is named per architect ADR); assert the chip is rendered with the «information only» label visible to assistive tech (`getByRole('status')` or similar). Render the same chart WITHOUT the chip flag; assert no chip is rendered.
2. **Mandatory caption** — render any chart that has a finance-mandated caption (e.g., treemap intra-day with staleness banner, drift-bar with rebalancing-context caption); assert the caption is rendered above the chart, not below.
3. **Projection rendering** — render a chart with a `projection` field set; assert the projection renders dashed (not solid) and a disclaimer caption is present.
4. **Currency formatting** — render a chart with `currency: 'EUR'`; assert tooltip values use locale-aware EUR formatting; render with `currency: 'USD'`; assert USD. (Architecture per architect ADR.)
5. **Lane-A vocabulary** — render a chart whose payload accidentally contains prescriptive vocabulary in `meta.factualCaption` (e.g., «you should rebalance»); assert the regex layer (per finance audit cross-cutting recommendation) catches it OR the parser rejected it earlier (preferred). The test verifies the defence, regardless of which layer wins.

For per-chart guardrails (e.g., scatter MVP gate, candlestick T3 gate, calendar V2 paid-data gate per finance audit findings):

6. **Scatter MVP gate** — attempt to render a `kind: 'scatter'` payload while the MVP feature flag is OFF; assert the chart-error state is rendered with the «V2 chart, not yet greenlit» message; assert the scatter component is NOT mounted (lazy-load did not trigger).
7. **Candlestick T3 gate** — same pattern for `kind: 'candlestick'` while the T3 feature flag is OFF.
8. **Calendar V2 gate** — same pattern for `kind: 'calendar'` configured with paid-data eventTypes that are V2-only.

### OUT of scope (deferred or owned elsewhere)

- E2E test of the AI agent → chat → chart-render flow. Owned by chat-flow QA, not this slice.
- Performance benchmarks (render time, FPS during animation). Tracked as a follow-on TD; not a block for chart MVP.
- Cross-browser visual regression (Firefox / Safari). MVP locks Chromium baselines only. Cross-browser is a follow-on slice.
- Accessibility audit (axe-core). Tracked separately under the accessibility kickoff path; do NOT bundle here.
- Mobile-viewport visual baselines. MVP locks 1280×800 only; mobile baselines deferred.

---

## 6. Implementation plan

1. **Setup** — verify Playwright + Vitest are configured for the new test paths. Add `tests/visual/` directory; add `pnpm test:visual` and `pnpm test:visual:update` scripts.
2. **Layer A — Parser tests** — write before FE merges. They test backend's schema directly; do not need FE rendering. Land first; provides confidence the contract is locked.
3. **Layer C — Runtime guardrails** — write second; can run against FE's components in jsdom. Land before visual regression so any guardrail bug is caught before screenshot baselines lock the wrong UI.
4. **Layer B — Visual baselines** — write last; capture only after Layer C confirms guardrails are correct. Otherwise visual baselines lock incorrect UI and we pay for the mistake on every future chart change.
5. **CI wiring** — three jobs: `test:parser`, `test:guardrails`, `test:visual`. Parser job runs on every PR; visual job runs only on PRs that touch `packages/ui/src/charts/**` or `packages/design-tokens/**` (via path filter to avoid pointless reruns).

---

## 7. Acceptance criteria (testable)

- [ ] Parser-tier tests cover all 11 chart kinds with at least one positive case each.
- [ ] Parser rejects every CRITICAL and HIGH violating payload identified in finance audit §3.
- [ ] Parser rejects each of the 3 Risk Flags from CHARTS_SPEC §5.2 with assertion on the Zod issue path.
- [ ] Visual regression captures 22 baselines (11 charts × 2 themes) at 1280×800.
- [ ] Each baseline uses a realistic fixture sourced from CHARTS_SPEC §5.3 (not synthetic).
- [ ] Visual diff threshold is 0.1%; any regression above threshold blocks CI.
- [ ] Runtime guardrails 1–8 (per §5 Layer C) all have at least one positive and one negative test case.
- [ ] Lazy-loaded T2 / T3 chart kinds (StackedBar / Scatter / Waterfall / Candlestick) do NOT mount when their feature flag is OFF; verified by mount-spy assertion.
- [ ] CI green. Zero flaky tests across 3 consecutive runs (`pnpm test --run --repeat-each 3` for the parser + guardrail jobs; visual job re-run on a clean checkout).
- [ ] Test names are descriptive and grouped by chart kind for fast triage on failure.
- [ ] No new dependencies beyond what FE + backend slices introduced.
- [ ] No paid SaaS, no Chromatic / Percy / Argos.
- [ ] Hard rules R1 / R2 / R4 cited verbatim in PR description.

---

## 8. Verification commands

Builder runs locally before PR:

```bash
# Parser + guardrails
pnpm --filter @investment-tracker/ui test --run

# Visual regression (initial capture)
pnpm test:visual:update
# Visual regression (verify against baselines)
pnpm test:visual

# Full suite
pnpm test
```

CI runs the same commands minus `:update`. PR is mergeable only when all three jobs are green and zero flaky retries are needed.

---

## 9. Cross-team flags

- **architect:** if your data-binding contract requires a test-helper for constructing well-formed `ChartEnvelope` fixtures (e.g., a `makeChartFixture(kind, overrides)` builder), expose it from the schema package as a test-only export. QA will consume; no production impact.
- **frontend-engineer:** if any chart's `data-testid` attributes are needed for runtime guardrail tests (e.g., locating the info-only chip), add them in your slice; QA tests assume `data-testid="chart-info-chip"`, `data-testid="chart-caption"`, `data-testid="chart-projection"`, `data-testid="chart-error-state"`. If your component naming differs, surface the delta in your PR description so QA tests sync.
- **backend-engineer:** parser tests live in your slice's PR (preferred) or in a sibling test file under `packages/shared-types/__tests__/` if your schema package owner-ship boundary makes that cleaner. Either is acceptable; clarify with Right-Hand on dispatch.
- **finance-advisor:** after this QA slice merges, you re-verify in Phase 3 that the runtime guardrails actually catch every CRITICAL / HIGH from your audit. If any audit finding maps to no test, that's a Phase 3 gap.

---

## 10. Open questions for tech-lead / right-hand

1. **Visual regression CI cost** — 22 baselines × 2 themes is small, but the Playwright job can be slow on cold cache. If the CI duration becomes a problem, Right-Hand may opt to run visual regression only nightly + on tag, with PRs running parser + guardrails only. Default = run on every PR for now; revisit if CI duration crosses ~5 min for the chart job.
2. **Fixture source** — CHARTS_SPEC §5.3 example payloads are AI-agent-generated examples and may contain mock data with unrealistic values (e.g., a 50% return on a 1-day chart). For visual baselines, prefer the catalog's per-surface realistic data when available; fall back to spec examples otherwise. If the realism gap is wide enough that baselines look unconvincing, surface to product-designer for a fixture-curation pass.

---

## Notes

This kickoff was completed by Right-Hand after tech-lead's dispatch session ended early due to a stream timeout. Tech-lead produced the FE + backend kickoffs and the theme-mechanism DECISIONS.md entry; the QA kickoff structure mirrors the FE / backend kickoff pattern they established. If tech-lead is re-engaged on chart work, this kickoff can be refined or superseded.
