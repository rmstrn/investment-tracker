meta {
  name: Create Conversation
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/ai/conversations
  body: json
  auth: bearer
}

headers {
  Idempotency-Key: {{$randomUUID}}
}

body:json {
  {
    "title": "Started from Bruno"
  }
}

vars:post-response {
  conversationId: res.body.id
}
