meta {
  name: Update Account
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/accounts/{{accountId}}
  body: json
  auth: bearer
}

headers {
  Idempotency-Key: {{$randomUUID}}
}

body:json {
  {
    "display_name": "Renamed IB Taxable"
  }
}
