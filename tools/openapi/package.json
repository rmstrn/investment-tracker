{
  "name": "@investment-tracker/openapi",
  "version": "0.0.0",
  "private": true,
  "description": "OpenAPI 3.1 specification and code generators for the Investment Tracker API.",
  "scripts": {
    "generate": "pnpm generate:ts && pnpm generate:go && pnpm generate:swift",
    "generate:ts": "bash ./generate-ts.sh",
    "generate:go": "bash ./generate-go.sh",
    "generate:swift": "bash ./generate-swift.sh",
    "lint": "pnpm exec redocly lint openapi.yaml",
    "preview": "pnpm exec @scalar/cli reference serve openapi.yaml --port 4040",
    "bundle": "pnpm exec redocly bundle openapi.yaml -o generated/openapi.bundled.json"
  },
  "devDependencies": {
    "@redocly/cli": "^1.34.12",
    "@scalar/cli": "^1.0.0"
  }
}
